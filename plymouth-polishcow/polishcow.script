for (i = 1; i <= 63; i++) {
    frames[i] = Image("frame-" + i + ".png");
}
spr = Sprite();
current = 1;

fun refresh_cb() {
    current = current + 1;
    if (current > 63) current = 1;
    spr.SetImage(frames[current]);
}
Plymouth.SetRefreshFunction(refresh_cb);
